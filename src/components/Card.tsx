import styled from "@emotion/styled";

const CardComponent = styled.div``

const Card = () => {
    return (
        <CardComponent>
            Card
        </CardComponent>
    )
  };

export default Card;
